"""Superseded by dispute_engine.categories — kept only so stale
imports fail loudly instead of silently resolving to old templates."""

raise ImportError(
    "buckets/dispute_letters were merged into dispute_engine.categories. "
    "Import DISPUTE_CATEGORIES from dispute_engine.categories instead."
)
