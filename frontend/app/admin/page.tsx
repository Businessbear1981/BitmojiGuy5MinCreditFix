export default function AdminPage() {
  return <div style={{ minHeight: '100vh', background: 'var(--black)', color: 'var(--white)', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'var(--font-heading)' }}>
    <p style={{ color: 'var(--gold)', fontSize: 24, letterSpacing: 2 }}>Admin Dashboard — Proxied from Flask /admin</p>
  </div>
}
