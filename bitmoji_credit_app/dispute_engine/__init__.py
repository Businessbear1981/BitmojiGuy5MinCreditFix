"""
Dispute Engine — v3 rebuild
Components:
  legal_library.py  — verified statutory + case law authorities
  parsing_engine.py — credit report parser → normalized data model
  analyst.py        — violation theory matching + item grouping
  letter_generator.py — comprehensive per-bureau letter composition
"""
